# 2026-05-05

## 今日の計画

- issue #2 の HTML renderer 実装を仕上げる
- snapshot test / README / CLI を揃える
- stacked branch として PR を作る

## やったこと

- `compile_gui_manifest_from_yaml` の JSON を renderer 向け typed model に変換する parser を追加した
- schema-only preview 用の `render_gui_html_from_manifest` と、runtime projection 用の `render_runtime_gui_html` を追加した
- `compile_gui_html_from_yaml` と CLI の `gui-html` mode を追加して、YAML から直接 HTML preview を出せるようにした
- manifest 経由 / runtime 経由の HTML snapshot test を追加した
- README と diary を更新して、issue #2 のスコープを static HTML renderer として明確にした
- issue #2 は `issue1-complete` の上に積んだ stacked branch なので、PR も `main` ではなく `issue1-complete` 向けに作る前提で整理した
- その後 issue #2 全体を見直すと、static HTML renderer は 1/7 くらいの前進でしかなく、最終 target を別に決める必要があると分かった
- 次の target として MoonBit WASM を選び、`wasm/demo/` package を追加して、WASM export + browser host HTML/JS で preview を表示する最小経路を作り始めた
- `wasm/demo` は DOM を MoonBit から直接触るのではなく、MoonBit 側は HTML 文字列を返し、host 側が `<iframe srcdoc>` に流し込む形にした。これなら既存の renderer と runtime bridge をそのまま再利用しやすい
- host page には YAML textarea も置き、example schema を編集して render し直せるようにした。これで compile/validation error を browser 上で観察できる
- host page の chrome は `en` / `ja` を切り替えられるようにし、i18n はまず browser shell から始めた。entity / field / state の label 自体はまだ YAML の単一文字列をそのまま通している
- さらに scenario 切替だけでは issue #2 の「実際の interaction」に届かないので、`DemoSession` を WASM export として持たせ、browser host から現在 state / available transitions / action input を見ながら `apply_transition` を実行できるようにした
- host 側は opaque な `DemoSession` を保持し、`session_snapshot_json` で action card を描画し、transition ごとの input を JSON で `apply_session_transition` に渡して iframe preview を再描画する。これで fixed scenario dropdown から一歩進んで、runtime state machine を browser から前に進められるようになった
- JS host では `use-js-builtin-string` 用の `_` import object も渡すようにして、WASM 初期化の前提も揃えた
- 次の slice では `validation_manifest` も host で読むようにして、action input ごとに `entity-field / local`、type、target/default、readOnly/system のようなスキーマ由来ヒントを card 上に出す方針にした。これで manifest が単なる raw JSON 表示ではなく、実際の host UI に効く metadata source になった
- transition 実行が失敗したときは global status だけでなく action card の近くにも error と直前 payload を残すようにした。これで「何を送って失敗したか」を browser 上で追いやすくなり、再入力もやり直しやすくなった
- さらに schema の `label` を単一文字列だけでなく locale map でも受けられるようにして、`default` / `ja` などから entity / field / state の表示名を解決する helper を追加した。`ja-JP` のような locale でも base language fallback を通せるよう、`ja-jp -> ja -> default -> 従来文字列/名前` の順で解決する
- `generate_gui_manifest` / `project_gui` / HTML renderer / `wasm/demo` の session snapshot まで locale を通すようにしたので、host 既存の `en` / `ja` 切替で shell chrome だけでなく schema 由来ラベルも同時に切り替わるようになった
- example schema も bilingual label map に更新し、MoonBit test には locale fallback と localized label の smoke test を追加した

## 学んだこと

- **stacked PR** は、前の PR の差分を土台に次の PR を積むやり方で、依存する変更を小さい単位でレビューしてもらいやすい
- **GUI manifest parser** を renderer 本体から分けると、HTML 以外の renderer を将来追加するときも JSON 読み取り責務を共有しやすい
- **snapshot test** は HTML のように文字列差分で壊れやすい成果物に向いていて、renderer の見た目の骨格をまとめて固定できる
- **WASM-GC** は JavaScript から MoonBit 関数を呼び出すのに向いているが、DOM API を MoonBit から直接全面的に扱うには JS glue がまだ多い。なので今回は MoonBit を preview engine、browser JS を mount glue として分けるのが自然だった

## 次にやること

- PR を作成して review 可能な状態にする
- `wasm/demo` を `moon build --target wasm-gc` で安定して配布できるようにする
- issue #2 の残りで、label i18n の次にどこを詰めるかを切り分ける
