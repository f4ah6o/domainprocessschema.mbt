# 2026-05-04

## 今日の計画

- issue #1 を実装可能な最小スコープに切り出す
- MoonBit プロジェクトを初期化する
- YAML -> 正規化モデル -> バリデーション -> SQL 生成の縦切りを作る
- CLI / example / test を揃える

### issue #2 の追加計画

- GUI manifest を renderer 向けの型に落とし込む
- MoonBit だけで静的 HTML preview を生成する
- runtime の `project_gui` 出力も HTML に橋渡しする
- manifest 経由 / runtime 経由の black-box test を足す
- README と CLI の使い方を更新する

## やったこと

- 空だった `domainprocessschema.mbt` を MoonBit module として初期化した
- repo 名には `.` が入っているが、MoonBit の module 名では `.` を使えないので `f4ah6o/domainprocessschema_mbt` にした
- issue #1 の YAML 例を扱える **制限付き YAML サブセット parser** を実装した
- parse 後に stringly typed な draft model を作り、その後で型・参照・状態遷移を検証する validator を追加した
- phase 1 の生成物として SQL DDL を出す generator を追加した
- `moonbitlang/x/fs` と `moonbitlang/x/sys` を使って CLI から schema file を読めるようにした
- example schema と black-box test を追加した
- その後の続きとして、`constraint` / `guard` / `rule` を raw string のまま扱わず、小さな式 AST に parse して検証するようにした
- SQL に続く 2 つ目の生成物として API / action manifest JSON を追加した
- CLI も `sql` と `api-manifest` を切り替えられるように広げた
- さらに issue #1 の残りを埋めるため、初期 migration SQL / validation manifest / GUI manifest も生成できるようにした
- これで 1 つの YAML 入力から DB schema / migration / API / validation / GUI 向け artifact をそれぞれ出力できる状態になった
- 次の段階として、validated `Schema` と `Expr` を直接使う **in-memory runtime** を追加し、record payload の検証・利用可能 transition の列挙・transition 適用までを MoonBit API として扱えるようにした
- runtime は generated JSON manifest をもう一度 parse するのではなく、compiler の内部表現をそのまま使う形にした
- black-box test では、default で state が入ること、constraint が失敗すること、manager role でだけ approve/reject が開くこと、submit/approve で state が進むことを固定した
- issue #1 を「本当に実装できているか」を見直したとき、`rejectReason` のような **transition 専用 input** が entity field だけでは表せないことに気づいたので、transition-local input 定義を追加した
- さらに rule を manifest に文字列で残すだけでは section 16 の「rule評価」「GUI生成」を満たしたと言いにくいので、`evaluate_rules` と `project_gui` を public API として追加し、現在 state から view を選び、利用可能 action と rule 状態を返す runtime GUI projection まで実装した
- これで issue #1 の「DB / API / Validation / GUI の生成」と「state / transition / rule に基づく実行モデル」が、少なくとも in-memory な形では一続きに実行できるようになった
- issue #2 の最初の slice として、`compile_gui_manifest_from_yaml` が返す JSON をそのまま renderer 入力にできるよう、GUI manifest 専用の typed model / parser を追加した
- renderer 本体は MoonBit の中で完結する **static HTML renderer** として実装し、entity header / state badge / visible fields / action button / transition-local input / rule 表示をまとめて 1 枚の HTML に落とすようにした
- `render_gui_html_from_manifest` / `compile_gui_html_from_yaml` / `render_runtime_gui_html` を public API に追加し、schema-only preview と runtime stateful preview の両方を呼べるようにした
- CLI に `gui-html` mode を追加したので、example YAML からそのまま HTML preview を出力できる
- runtime 側は既存の `project_gui` を再利用しているので、state ごとの visible field や manager role でだけ開く action の差分がそのまま HTML に反映される
- black-box test では、GUI manifest からの静的 preview と、`submit` 後の record を manager 視点で描画する stateful preview の 2 経路を固定した
- 今回は terminal 中心の作業だったので画像のスクリーンショットは置いていないが、その代わり snapshot test と `gui-html` CLI 出力を目視しやすい proof として残した

## 学んだこと

- **SSOT** は Single Source of Truth の略で、同じ情報を何度も別形式で持たず、一つの定義から複数の成果物を生成する考え方を指す
- 今回はいきなり API や GUI まで広げず、まず SQL という具体的な生成結果に絞ることで、schema IR の妥当性を検証しやすくした
- MoonBit では package ではなく directory が実際の compilation unit なので、同じ package の中なら file を分けても一つの実装として扱える
- **expression AST** は文字列をそのまま各 generator で再解釈するのではなく、一度意味のある木構造に変換してから共有する設計で、generator 間のズレを減らしやすい
- **API/action manifest** はまだ server code ではないが、entity と transition から route / action を機械可読な形で固定する中間成果物として有効
- **migration SQL** は schema 本体と別に up/down の境界を持つので、初期導入時の DB 反映手順を artifact として持ちやすい
- **GUI manifest** は実 UI コードそのものではないが、view ごとの field mode / action / enabled 条件を固定できるので renderer の入力として使いやすい
- **in-memory runtime** は HTTP server ではなく、まずは library として record と actor を受け取って判定する層を作る、という意味にした。これにより transport や storage を決める前に業務ルールを固められる
- generated manifest を再度 parse せず validated `Schema` / `Expr` を直接使うと、generator と runtime で意味のズレが起きにくい
- **transition-local input** は、transition 実行のために一時的に受け取る値を指す。これは entity に保存される field とは別物なので、`input` の名前一覧と `inputs` の型定義を分ける方が素直だった
- **runtime GUI projection** はフロントエンドそのものではなく、今の state でどの view を見るか、どの field を editable / readonly / hidden にするか、どの action が押せるか、どの rule が有効かを計算した結果オブジェクトのこと
- PR review を受けて、`project_gui` 実行時ではなく validator の時点で **state と view の対応漏れ** を落とすようにした。compile が通ったのに runtime で初めて壊れる、という手戻りを減らせる
- transition-local input の `required` は、`default` があるのに常に必須扱いだと不自然なので、`required` を明示できるようにしつつ、未指定時は `default` の有無から自然に決めるようにした
- **GUI manifest parser** は、renderer が raw JSON 文字列に直接依存せず、型付きの中間表現を経由するための層。ここを分けておくと、将来 React/Vue など別 renderer を足すときも HTML renderer と parser を分離しやすい
- **static HTML renderer** はブラウザで state を更新するアプリではなく、現時点の schema / runtime projection を説明可能な文書として固定するもの。これにより「GUI 生成」と言っても、まずは仕様確認しやすい artifact から始められる

## 次にやること

- runtime を HTTP/server 層や persistence 層に接続する
- HTML renderer に軽い style / accessibility 調整を入れる
- static HTML を将来の interactive frontend へどう接続するか決める
- migration 差分生成や schema diff をどう扱うか決める
- YAML parser の対応範囲を広げるか、専用 dependency に切り替えるか判断する
